---
import ServiceGaranti from "../components/ServiceGaranti.astro";
import Accordion from "../components/Accordion.astro";
import MainLayout from "../layouts/MainLayout.astro";
---
<MainLayout>

  <div class="relative w-full overflow-hidden bg-neutral-500">
    <svg id="svg-mask" class="absolute top-0 left-0 w-full h-full">
        <defs>
            <clipPath id="clip">
                <circle cx="20%" cy="20%" r="0" id="circle-mask" />
            </clipPath>
        </defs>
    </svg>
    <div class="relative top-0 left-0 bg-gradient-to-r from-[#16312B] to-[#0C1C19] transition-all duration-300 ease-out " style="clip-path: url(#clip)">

<div class="bg-primary-500 lg:p-l lg:pl-20">
  <div class="flex flex-col lg:flex-row pt-ml py-lg px-ml lg:p-2xl text-accent-500">
    <!-- Text Section (Heading, Image, and Paragraph) -->
    <div class="flex flex-col lg:max-w-md lg:pr-0">
      <!-- Heading -->
      <h2 class="text-h2-mobile md:text-h2-tablet lg:text-h2-desktop pb-sm lg:pb-xl">
        <span>Teamet bag </span><span>Maltclub</span>
      </h2>
      
      <!-- Image Section -->
      <div class="flex justify-center lg:hidden mb-4">
        <img 
          src="img/img_profil/malt_team.webp" 
          alt="profilbilleder af virksomhedens ejere" 
          class="w-96 h-96">
      </div>

      <!-- Paragraph -->
      <p class="text-body-md md:text-body-md-tablet lg:text-body-md-desktop font-serif">
        Hos MaltClub brænder vi for at dele passionen for øl med historie og kvalitet. Hver måned sammensætter vi en kasse med spændende øl fra bryghuse med noget på hjerte – altid ledsaget af inspirerende fortællinger om øllet og bryghuset.
        <br><br>Vi glæder os til at pirre dine smagsløg med næste MaltClub ølpakke!
      </p>
    </div>

    <!-- Image Section for Desktop -->
    <div class="hidden lg:flex justify-center lg:ml-48 mt-6 lg:mt-0">
      <img 
        src="img/img_profil/malt_team.webp" 
        alt="profilbilleder af virksomhedens ejere" 
        class="lg:w-[28rem] lg:h-[28rem]">
    </div>
  </div>
</div>



<ServiceGaranti />

 <div class="flex flex-col pt-ml py-lg px-ml lg:p-2xl bg-primary-500">
    <div class="flex items-center space-x-2 text-accent-500">
        <!-- SVG Icon -->
        <div class="lg:pl-20">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" width="24" height="24" stroke-width="2" stroke-linejoin="round" stroke-linecap="round" stroke="currentColor">
            <path d="M5 12l14 0"></path>
          </svg></div>
        <!-- Text -->
        <p class="font-serif text-body-md md:text-body-md-tablet lg:text-body-md-desktop text-accent-500 lg:pb-l">Vi er her for at hjælpe</p>
      </div>
    <h3 class=" text-accent-500 lg:pb-xl lg:pl-20 ">Ofte stillede spørgsmål</h3>
    
  <div>
  <Accordion 
    question="Hvordan bestiller jeg et Maltclub abonnement?"
    answer="Du bestiller et MaltClub abonnement lige her."
    index="1" />


  <Accordion 
    question="Hvor lang tid binder jeg mig?"
    answer="Vi har ingenbinding hos MaltClub, og du kan til enhver tid opsige dit abonnement. Vær dog opmærksom på at dit abonnement skal opsiges senest den 20. i måneden for at være gældende fra den efterfølgende måned."
    index="2" />

  <Accordion
    question="Hvor meget koster et Maltclub abonnemet?"
    answer="Et MaltClub abonnement med levering hver måned koster fra 275 kr. pr. md. inkl. fragt. Du kan få 20% i rabat på den første måned ved tilmelding til vores nyhedsbrev. "
    index="3" />

  <Accordion
    question="Hvordan kan jeg betale på Maltclub?"
    answer="På MaltClub.dk kan du betale med Dankort, Visakort og Masterkort, når du opretter et abonnement. Betaling med debet-eller kreditkort er gebyrfrit (dog kan der forekomme gebyr på betaling med firmakort)."
    index="4" />

  <Accordion 
    question="Hvordan opsiger jeg mit abonnement?"
    answer="Ønsker du at opsige dit abonnement bedes du kontakte os på info.maltclub@gmail.com eller via vores kontaktformular her. Du modtager herefter en skriftlig bekræftelse på din opsigelse."
    index="5" />
  </div>
 </div>
</div>
</div>
</MainLayout>


<script>
  import { animate } from "motion";
  window.addEventListener('load', () => {
    const circleMask = document.getElementById('circle-mask');
    
    // Define the center and radius
    const centerX = 50;  // Center of the circle (in percentage)
    const centerY = 20;  // Center of the circle (in percentage)
    const radius = 10;   // Radius in percentage
    let angle = 0;       // Initial angle (in radians)
    
    // Animate the circle by first scaling it slightly
    animate(circleMask, 
      { 
        r: ['0', '10%'],  // Start with radius 0, then scale to 10% size
      },
      {
        duration: 0.3,      // Duration of the scaling animation
        
      });

    // Use setTimeout to delay the movement by 1.4 seconds (the duration of the scaling animation)
    setTimeout(() => {
      // Animate circular motion continuously using requestAnimationFrame
      function animateCircularMotion() {
        // Calculate the new position based on the angle
        const cx = centerX + radius * Math.cos(angle);  // X position (percentage)
        const cy = centerY + radius * Math.sin(angle);  // Y position (percentage)

        // Update the circle's position
        animate(circleMask, 
          { 
            cx: `${cx}%`,  // Update horizontal position (in percentage)
            cy: `${cy}%`,  // Update vertical position (in percentage)
          },
          {
            duration: 0.0005,       // Shortened duration for faster movement
                // Smooth and continuous movement
          });

        // Increment the angle for continuous movement
        angle += 0.07;  // Increased angle increment for faster rotation

        // Continue the animation
        if (angle <= 2 * Math.PI) {
          requestAnimationFrame(animateCircularMotion);
        } else {
          // Once the full circle is completed, stop and scale to full size
          animate(circleMask, 
            { r: ['10%', '150%'] },  // Scale from 10% to 150%
            {
              duration: 1,        // Slightly reduced scaling duration
            });
        }
      }
  
		// Start the continuous animation
		animateCircularMotion();
	  },
	)});
</script>